{
  "name": "unit-testing-demos-with-vitest",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest --reporter verbose",
    "test:watch": "vitest --reporter verbose --watch",
    "test:ui": "vitest --ui"
  },
  "devDependencies": {
    "@vitest/ui": "3.2.4",
    "vite": "^7.1.7",
    "vitest": "^3.2.4"
  }
}
